# Basic server port configuration
server.port=8080

# Spring Boot application name
spring.application.name=coderator-backend

H2 Database (in-memory for development - remove or change for production)
# This will create an in-memory database accessible via http://localhost:8080/h2-console
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console
# spring.datasource.url=jdbc:h2:mem:coderator_db
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=password

# JPA (Java Persistence API) settings
# spring.jpa.hibernate.ddl-auto=update # 'update' creates/updates schema, 'create' drops and recreates, 'none' does nothing
# spring.jpa.show-sql=true # Show SQL queries in console
# spring.jpa.properties.hibernate.format_sql=true # Format SQL queries for readability
